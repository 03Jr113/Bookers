<h1>Editing Book</h1>

<%= form_with model: @book do |f| %>

<%= f.label :title, 'Title' %>
<%= f.text_field :title %>

<%= f.label :body, 'Body' %>
<%= f.text_area :body %>

<%= f.submit 'Update Book' %>

<% end %>